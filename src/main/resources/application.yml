server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/korvata/kotlin_msa_config
management:
  endpoints:
    web:
      exposure:
        include: health, busrefresh
